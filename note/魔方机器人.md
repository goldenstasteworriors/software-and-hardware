## 魔方机器人

### TO DO:

1. 全部改为绝对位置
2. 到位自动下一步(需要把while改掉)
3. 力矩模式





1.8度电机

绝对位置模式：位置=1.8/细分数*脉冲数

归位代码不包含松开

所有的延时都在case里

位置模式:



1. 4_19:测试以下单圈方向回零等是怎么回的



#define   En_Pin1     5   // 外夹爪使能引脚
#define   Stp_Pin1    6   // 外夹爪脉冲引脚
#define   Dir_Pin1    7   // 外夹爪方向引脚

#define   En_Pin2     8   // 外夹爪使能引脚
#define   Stp_Pin2    9   // 外夹爪脉冲引脚
#define   Dir_Pin2    10   // 外夹爪方向引脚



电机/驱动板ID：

靠我们的夹爪旋转：1

靠里的夹爪旋转：2

靠我们的夹爪夹紧：3

靠里的夹爪旋转：4



方向:

1. 夹紧爪子:双CW：打开



连arduino的：黑的连接rx，白的连tx



### 初始化 

1. 初始化时变



#### 数值(均为脉冲数)

1. 4.26:没装垫片最内侧到可以放魔方:160；到最外侧:约14*80=1120
2. 

![image-20250420190316537](../../pictrue/image-20250420190316537.png)



![image-20250420190300109](../../pictrue/image-20250420190300109.png)





### 上位机通信

![img](../../pictrue/89413d0d030352ba7476782189794fa6.png)



left:靠外

right:靠内



```
    case '1':  // 电机1位置控制
      Emm_V5_Pos_Control(MOTOR1_ADDR, 0, 100, 0, 0, 1, 0);//(uint8_t addr, uint8_t dir, uint16_t vel, uint8_t acc, uint32_t clk（脉冲数）, bool raF(相位/绝对标志，false为相对运动，true为绝对值运动), bool snF)
      break;

    case '2':  // 电机1位置控制
      Emm_V5_Pos_Control(MOTOR1_ADDR, 0, 100, 0, 1600, 1, 0);
      break;

    case '3':  // 电机2位置控制
      Emm_V5_Pos_Control(MOTOR2_ADDR, 0, 100, 0, 1600, 0, 0);
      break;

    case '4':  // 电机2位置控制
      Emm_V5_Pos_Control(MOTOR2_ADDR, 1, 100, 0, 1600, 0, 0);
      break;
      
    case '5':  // 电机3 正传
      Emm_V5_Pos_Control(MOTOR3_ADDR, 0, 100, 0, 160, 0, 0);//(uint8_t addr, uint8_t dir, uint16_t vel, uint8_t acc, uint32_t clk（脉冲数）, bool raF(相位/绝对标志，false为相对运动，true为绝对值运动), bool snF)
      break;
    case '6':  // 电机3 反转
      Emm_V5_Pos_Control(MOTOR3_ADDR, 1, 100, 0, 160, 0, 0);
      break;
    case '7':  // 电机4 正转
      Emm_V5_Pos_Control(MOTOR4_ADDR, 0, 100, 0, 160, 0, 0);
      break;
    case '8':  // 电机4 反转
      Emm_V5_Pos_Control(MOTOR4_ADDR, 1, 100, 0, 160, 0, 0);
      break;
    case 'z':  // 电机1,2多机同步
      Emm_V5_Pos_Control(MOTOR1_ADDR, 0, 100, 0, 160, 0, 1);//最后一个取1是多机同步，倒数第二个取1是绝对运动
      delay(50);
      Emm_V5_Pos_Control(MOTOR2_ADDR, 0, 100, 0, 160, 0, 1);
      delay(50);
      Emm_V5_Synchronous_motion(0);
      break;
    case '9': //电机2逆时针180度
      // Emm_V5_Pos_Control(MOTOR1_ADDR, 0, 100, 0, 1600, 1, 0);
      delay(50);
      Emm_V5_Pos_Control(MOTOR2_ADDR, 1, 100, 0, 1600, 0, 1);
      delay(50);
      Emm_V5_Pos_Control(MOTOR4_ADDR, 1, 100, 0, 1600, 0, 1);
      delay(50);
      Emm_V5_Synchronous_motion(0);
      break;
```



### 5.26新爪子(绿)参数

打开到90度时不碰到:1000；为保守取960



4.87



#### debug日志

1. 5.8:发现单位是度而不是脉冲数；初始化等给电机发消息只会返回

   ```
   21:49:12.887 -> Fik�kFik�kFik�kFik�k�k�kready
   21:49:13.964 -> �,�k�,�k
   ```

   而不会运动

2. 5_29:arduino前后夹爪在松紧几次之后会越来越松，上位机控制没有这个情况

   上位机串口信息：01 FD 00/01 00 32 00 00 00 03 AC 00 00 6B (CW,松/CCW，紧)

   

3. 
