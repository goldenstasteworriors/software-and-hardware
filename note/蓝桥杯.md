24Mz，要用HSE

用cubemx的STM32Cube_FW_G4_V1.4.0来用作固件地址

安装pack包



点亮LED等要load引脚PD2输出高电平

led要初始化

一定要记得看中断回调函数对了没有

按键要上拉

有line0记得看行数对应的line

用类似%-8d来左对齐并指定位数，类似%-10.3f同时指定宽度小数点位数和对齐

IC:HAL_TIM_ReadCaptureValue然后要重新开一次

__HAL_TIM_SetCompare

ADC是PB15和PB12

adc要先start

eeprom读是1写是0;注意在读eeprom时流程是先写地址(此时是写，0)，然后切换为读(1)

eeprrom读写函数后面加延时



串口用strlen



问题：输入捕获测一次测不准