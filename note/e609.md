@[toc]

### e609





#### 3_24上午:

夏天学长介绍：用众擎框架调开普勒机器人，用的legged_gym改的；跑通例程后到servo01上copy夏天学长的legged_robot.config/legged_robot.config;resource中的





core_dumped多半是因为pytorch版本问题

vscode折叠：(ctrl+K)+(ctrl+0(不能用小键盘的))           展开：(ctrl+K)+(ctrl+J) 

server01:317568604   server01 

换GPU：export CUDA_VISIBLE_DEVICES=2

hugging-face:`huggingface-cli download --repo-type dataset + 数据集 --local-dir <本地路径>` 

删除所有unitree进程：`ps aux | grep 'legged_gym' | awk '{print $2}' | xargs kill -9`

查看占用显存较多的进程：`nvidia-smi --query-compute-apps=pid,process_name,used_memory --format=csv,noheader,nounits | awk -F',' '{printf "PID: %-8s Process: %-30s VRAM: %s MiB\n", $1, $2, $3}' | sort -k5 -nr | head -n 5`

## unitree_rl_gym

在on_policy_runner.py中把log_dir=/data5/ykj/unitree_output/3_26了

python legged_gym/scripts/play.py --task=g1 --load_run 3_26 --checkpoint 7100

==nohup python legged_gym/scripts/train.py --task=g1 --headless  > log/walk_23dof_alive0_5_termination.log 2>&1 &==

用rev1



python deploy/deploy_mujoco/deploy_mujoco.py g1.yaml

ps aux | grep '[a]wd' | awk '{print $2}' | xargs kill -

显示的dict就是在legged_robot.py中extras赋值得到的(在logger中显示)



env是g1_env.G1ROBOT

更新command的方式就是`legged_robot里post_physics_step调用_post_physics_step_callback，在step到了要command_resample的时候随机给一个`

x是面朝方向，y是横移



### 计划

1. 4_16-4_20：改unitree_rl_gym使g1能正常走路，尝试路径跟踪
2.  
3. 





### Debug日志

1. 3_27:权重

   ```
               tracking_lin_vel = 1.0
               tracking_ang_vel = 0.5
               lin_vel_z = -2.0
               ang_vel_xy = -0.05
               orientation = -1.0
               base_height = -10.0
               dof_acc = -2.5e-7
               dof_vel = -1e-3
               feet_air_time = -1.0
               collision = 0.0
               action_rate = -0.01
               dof_pos_limits = -5.0
               alive = 0.15
               hip_pos = -1.0
               contact_no_vel = -0.2
               feet_swing_height = 0.0
               contact = 0.18
               feet_contact = 0.2
   ```

   在5000/5500时效果最好，后面就开始走起来而且不稳了

   5000左右：

   ```
                          Computation: 25305 steps/s (collection: 2.634s, learning 1.251s)
                  Value function loss: 0.0057
                       Surrogate loss: -0.0071
                Mean action noise std: 0.46
                          Mean reward: 33.90
                  Mean episode length: 1002.00
         Mean episode rew_action_rate: -0.0749
               Mean episode rew_alive: 0.1503
          Mean episode rew_ang_vel_xy: -0.0344
         Mean episode rew_base_height: -0.0013
             Mean episode rew_contact: 0.2023
      Mean episode rew_contact_no_vel: -0.0204
             Mean episode rew_dof_acc: -0.0460
      Mean episode rew_dof_pos_limits: -0.0057
             Mean episode rew_dof_vel: -0.0425
       Mean episode rew_feet_air_time: 0.0125
        Mean episode rew_feet_contact: 0.3671
             Mean episode rew_hip_pos: -0.0204
           Mean episode rew_lin_vel_z: -0.0266
         Mean episode rew_orientation: -0.0039
             Mean episode rew_torques: -0.0183
    Mean episode rew_tracking_ang_vel: 0.2903
    Mean episode rew_tracking_lin_vel: 0.9091
   ```

   

   把权重改为又训了一个(_2,无后缀的惩罚项更狠)：

   ```
               tracking_lin_vel = 1.0
               tracking_ang_vel = 0.0
               lin_vel_z = 0.0
               ang_vel_xy = -0.0
               orientation = -1.0
               base_height = -5.0
               dof_acc = -2.5e-7
               dof_vel = -1e-3
               collision = 0.0
               action_rate = -0.01
               dof_pos_limits = -5.0
               alive = 0.3
               hip_pos = -0.5
               contact = 0.2
   ```

   

2. 4_9:换成29dof后发现什么都学不到，后发现可能因为dof数量变动导致dof_vel/dof_acc主导训练；将与dof数量有关的dof_vel/dof_acc和torque分别改为原来的1/2,3/1,1/4后发现只有1/4的rew_alive会稍微高一点，其他的会直接训崩导致什么都学不到，似乎开始训练的时候非常脆弱

   12dof曲线![image-20250409150224309](../../pictrue/image-20250409150224309.png)

   23dof曲线(divide10)

   ![image-20250409150257026](../../pictrue/image-20250409150257026.png)

   

3. 23dof_alive5_devide10图像非常好但是效果非常差，令人费解

4. 4_16：傻逼了，之前的问题是由于没给pd参数，给了之后才能控制每个关节_

2. 4_18:加了termination之后都能训练出来，都是termination条件给的太宽松了导致机器人纷纷转而下地





### rsl_rl

actor_critic就是几层卷积:

```
        actor_layers = []
        actor_layers.append(nn.Linear(mlp_input_dim_a, actor_hidden_dims[0]))
        actor_layers.append(activation)
        for l in range(len(actor_hidden_dims)):
            if l == len(actor_hidden_dims) - 1:
                actor_layers.append(nn.Linear(actor_hidden_dims[l], num_actions))
            else:
                actor_layers.append(nn.Linear(actor_hidden_dims[l], actor_hidden_dims[l + 1]))
                actor_layers.append(activation)
        self.actor = nn.Sequential(*actor_layers)

        # Value function
        critic_layers = []
        critic_layers.append(nn.Linear(mlp_input_dim_c, critic_hidden_dims[0]))
        critic_layers.append(activation)
        for l in range(len(critic_hidden_dims)):
            if l == len(critic_hidden_dims) - 1:
                critic_layers.append(nn.Linear(critic_hidden_dims[l], 1))
            else:
                critic_layers.append(nn.Linear(critic_hidden_dims[l], critic_hidden_dims[l + 1]))
                critic_layers.append(activation)
        self.critic = nn.Sequential(*critic_layers)
```





### H2O

get_motion_state:返回一个字典，含有(按顺序)：root_pos，root_rot，dof_pos，root_vel，root_ang_vel，dof_vel，motion_aa(运动的轴角表示(Axis-Angle)),rg_pos(所有刚体的全局位置),rb_rot(刚体的全局旋转(四元数表示)),body_vel(刚体的线速度),body_ang_vel,motion_bodies(运动中涉及的刚体的索引),motion_limb_weights(运动中各个肢体的权重,用于加权计算运动的贡献，可能与奖励函数或优化目标相关) ==(还有几个带_t后缀的是扩展的，属于可选的)

teacher训练

```
nohuo python legged_gym/scripts/train_hydra.py  --config-name=config_teleop task=g1:teleop run_name=OmniH2O_TEACHER env.num_observations=913 env.num_privileged_obs=990 motion.teleop_obs_version=v-teleop-extend-max-full motion=motion_full motion.extend_head=True num_envs=4096 asset.zero_out_far=False asset.termination_scales.max_ref_motion_distance=1.5 sim_device=cuda:0 motion.motion_file=/home/yekangjie/Downloads/21_G2O/human2humanoid/data/g1/test.pkl rewards=rewards_teleop_omnih2o_teacher rewards.penalty_curriculum=True rewards.penalty_scale=0.5 headless=True > log/last.log 2>&1 &
```



retarget流程:

1. 读取模型，设定extend joint等参数，用于正运动学：Write forward kinematics of `H1` in `human2humanoid/phc/phc/utils/torch_h1_humanoid_batch.py` 
2. 用上一步的正运动学训练选定keypoint最接近机器人的SMPL的beta,theta参数：Fit the SMPL shape that matches the `H1` kinematics in `human2humanoid/scripts/data_process/grad_fit_h1_shape.py`
3. Retarget the AMASS dataset based on the corresponding keypoints between fitted SMLP shape and `H1` using `human2humanoid/scripts/data_process/grad_fit_h1.py`。（直接vis这里应该也能大致看到retarget的结果，因为红球是按retarget出来的joint画的，可以看看和机器人动的时候关节匹不匹配）(但是好像motion也是按照torch_g1_humanoid_batch.py得到的可能比较难可视化出区别)

h1向g1的迁移:

1. 读取模型的`torch_g1_humanoid_batch.py`:旋转矩阵的修改；修改extend的头和手手臂长度等；增删link,joint
2. motion_lib_g1除了调的换成了torch_g1_humanoid_batch.py都没改
3. grad_fit_g1_shape:joint_name;与smpl_joint_pick对应的g1_joint_pick因为link不同要改；h1换g1
4. grad_fit_g1:



实现：

1. test的时候，motion.reample=False,每个motion相同概率被执行；否则有一个采样频率

2. privileged_obs:![image-20250424203958650](../../pictrue/image-20250424203958650.png)

   代码:(全是刚体值，不是dof)

   ```
                   self_obs = compute_humanoid_observations_max_full(body_pos_extend, body_rot_extend, body_vel_extend, \																	 body_ang_vel_extend, True, False) # 342
                   # 22 * 3 + 23 * 6 + 23 * 3 + 23 * 3  = 342 | pos, rot, vel, ang_vel
                   task_obs = compute_imitation_observations_max_full(root_pos, root_rot, body_pos_subset, body_rot_subset, 																	body_vel_subset, body_ang_vel_subset,  \ 																			   ref_rb_pos_subset, ref_rb_rot_subset, 	\																			   ref_body_vel_subset, ref_body_ang_vel_subset,   \
                                                                      self.cfg.motion.num_traj_samples, ref_episodic_offset = 																				self.ref_episodic_offset)
                    # 23 * 3 + 23 * 6 + 23 * 3 + 23 * 3 + 23 * 3 + 23 * 6  = 552 diff pos, rot, vel, ang_vel | pos, rot
                   # Teacher
                   obs = torch.cat([ self_obs, 
                                               task_obs,  # 
                                               self.actions], dim = -1) # 342 + 552 + 19 = 913
   ```

   

3. omnih2o的observation(3key point,keypoint数量影响motion goal:motion goal=9*num_key point):![image-20250424201357133](../../pictrue/image-20250424201357133.png)

   在代码中是(后面要加noise)

   ```
                       obs = torch.cat([dof_pos, dof_vel, base_ang_vel, base_gravity,  
                                                   task_obs,  # 这个就是上面的motion goal,实际上包含diff_local_body_pos_flat, 																	local_ref_body_pos,local_ref_body_vel
                                                   self.actions, # 19dim
                                                   history_to_be_append], dim = -1) 
   ```

   上面的diff_local_body_pos_flat是参考与机器人的差，local_ref_body_pos是参考各keypoint与其root的差，local_ref_body_vel是速度差，跟论文说的一样：![image-20250424234925917](../../pictrue/image-20250424234925917.png)

   

1. 



教师策略（与学生策略相同部分均已删去）：

`run_name=OmniH2O_TEACHER env.num_observations=913 env.num_privileged_obs=990 motion.teleop_obs_version=v-teleop-extend-max-full motion=motion_full   rewards=rewards_teleop_omnih2o_teacher rewards.penalty_curriculum=True rewards.penalty_scale=0.5`

学生策略：

`run_name=OmniH2O_STUDENT env.num_observations=1665 env.num_privileged_obs=1742 motion.teleop_obs_version=v-teleop-extend-vr-max-nolinvel motion.teleop_selected_keypoints_names=[]     rewards=rewards_teleop_omnih2o_teacher rewards.penalty_curriculum=True rewards.penalty_scale=0.5 train.distill=True train.policy.init_noise_std=0.001 env.add_short_history=True env.short_history_length=25 noise.add_noise=False noise.noise_level=0 train.dagger.load_run_dagger=TEACHER_RUN_NAME train.dagger.checkpoint_dagger=XXX train.dagger.dagger_only=True`



privileged

```
python legged_gym/scripts/train_hydra.py --config-name=config_teleop task=h1:teleop run_name=OmniH2O_TEACHER env.num_observations=913 env.num_privileged_obs=990 motion.teleop_obs_version=v-teleop-extend-max-full motion=motion_full motion.extend_head=True num_envs=4096 asset.zero_out_far=False asset.termination_scales.max_ref_motion_distance=1.5 sim_device=cuda:0 motion.motion_file=resources/motions/h1/stable_punch.pkl rewards=rewards_teleop_omnih2o_teacher rewards.penalty_curriculum=True rewards.penalty_scale=0.5
```



sim2real

```
python legged_gym/scripts/train_hydra.py --config-name=config_teleop task=h1:teleop run_name=OmniH2O_STUDENT env.num_observations=1665 env.num_privileged_obs=1742 motion.teleop_obs_version=v-teleop-extend-vr-max-nolinvel motion.teleop_selected_keypoints_names=[] motion.extend_head=True num_envs=4096 asset.zero_out_far=False asset.termination_scales.max_ref_motion_distance=1.5 sim_device=cuda:0 motion.motion_file=resources/motions/h1/stable_punch.pkl rewards=rewards_teleop_omnih2o_teacher rewards.penalty_curriculum=True rewards.penalty_scale=0.5 train.distill=True train.policy.init_noise_std=0.001 env.add_short_history=True env.short_history_length=25 noise.add_noise=False noise.noise_level=0 train.dagger.load_run_dagger=TEACHER_RUN_NAME train.dagger.checkpoint_dagger=XXX train.dagger.dagger_only=True
```

sim2real--0stepMLP

```
python legged_gym/scripts/train_hydra.py --config-name=config_teleop task=h1:teleop run_name=OmniH2O_STUDENT_0stepMLP env.num_observations=90 env.num_privileged_obs=167 motion.teleop_obs_version=v-teleop-extend-vr-max-nolinvel motion.teleop_selected_keypoints_names=[] motion.extend_head=True num_envs=4096 asset.zero_out_far=False asset.termination_scales.max_ref_motion_distance=1.5 sim_device=cuda:0 motion.motion_file=resources/motions/h1/stable_punch.pkl rewards=rewards_teleop_omnih2o_teacher rewards.penalty_curriculum=True rewards.penalty_scale=0.5 train.distill=True train.policy.init_noise_std=0.001 env.add_short_history=False noise.add_noise=False noise.noise_level=0 train.dagger.load_run_dagger=TEACHER_RUN_NAME train.dagger.checkpoint_dagger=XXX train.dagger.dagger_only=True
```



#### humuoidverse框架

获取action:(actor_obs用哪些在obs选择的config文件夹里的yaml指定)

1. ppo_module的class PPOActor的`act(self, actor_obs, **kwargs)`,能找到最底层的是

   ```
   def update_distribution(self, actor_obs):
        mean = self.actor(actor_obs)
        self.distribution = Normal(mean, mean*0. + self.std)
   ```

2. agent里的ppo的`_actor_act_step -->_rollout_step`得到action存在actor_state里

3. 后面step取actor_state里的action来用`obs_dict, rewards, dones, infos = self.env.step(actor_state)`

4. 



### ASAP

要先export PYTHONPATH=$PYTHONPATH:/home/yekangjie/Downloads/ASAP

```
python humanoidverse/eval_agent.py \
+checkpoint=logs/MotionTracking/xxxxxxxx_xxxxxxx-MotionTracking_CR7-motion_tracking-g1_29dof_anneal_23dof/model_5800.pt
```



HYDRA_FULL_ERROR=1 python humanoidverse/train_agent.py  +simulator=isaacgym +exp=locomotion +domain_rand=NO_domain_rand  +rewards=loco/reward_g1_locomotion +robot=g1/g1_29dof_anneal_23dof +terrain=terrain_locomotion_plane +obs=loco/leggedloco_obs_singlestep_withlinvel num_envs=1 project_name=TestIsaacGymInstallation experiment_name=G123dof_loco headless=True

```
nohup python humanoidverse/train_agent.py \
+simulator=isaacgym \
+exp=motion_tracking \
+domain_rand=domain_rand_base \
+rewards=motion_tracking/reward_motion_tracking_dm_2real \
+robot=g1/g1_29dof_anneal_23dof \
+terrain=terrain_locomotion_plane \
+obs=motion_tracking/deepmimic_a2c_nolinvel_LARGEnoise_history \
num_envs=8192 \
+checkpoint="/home/yekangjie/Downloads/ASAP/logs/model_38000.pt" \
project_name=MotionTracking \
experiment_name=MotionTracking_CR7_domainrand \
robot.motion.motion_file="humanoidverse/data/motions/g1_29dof_anneal_23dof/TairanTestbed/singles/0-TairanTestbed_TairanTestbed_CR7_video_CR7_level1_filter_amass.pkl" \
rewards.reward_penalty_curriculum=True \
rewards.reward_penalty_degree=0.00001 \
env.config.resample_motion_when_training=False \
env.config.termination.terminate_when_motion_far=True \
env.config.termination_curriculum.terminate_when_motion_far_curriculum=True \
env.config.termination_curriculum.terminate_when_motion_far_threshold_min=0.3 \
env.config.termination_curriculum.terminate_when_motion_far_curriculum_degree=0.000025 \
robot.asset.self_collisions=0 > log/4_28_siu_domainrand.log 2>&1 &
```



python humanoidverse/eval_agent.py \
+checkpoint=logs/siu/model_5800.pt



==sim2sim==

1. limit:训练时关节的limit实际上是直接读的xml里的range；但是实际上强制要用的只有torque，其它是用在reward和termination
2. ref_motion_phase变化值的确定(以siu为例):pkl:1/30*119=4s;(dance,walk:320)0
2. 要给地面加一个摩擦力









### 各个urdf区别

1. 23dof_rev1.0和23dof:

   1. left/right_hip_roll_joint的effort和velocity
   2. 23dof多了waist_yaw_fixed_link，waist_support_link
   3. torso_link很不一样，用的stl也不一样
   4. head_joint，left/right_shoulder_pitch_joint的z略有区别

2.  23dof与29dof_aneal_23dof:(两个joint改为fixed，新增两个revolute 的joint，所以dof还是23没变，但是顺序是会变的，改的dof用处也并不完全一样，所以不能直接套用)

   29相比23：

   1. 删掉了`joint name="waist_yaw_fixed_joint" type="fixed";imu_in_pelvis`

   2. 增加了

      link:

      1. link name="waist_roll_link
      2. joint name="waist_roll_joint" type="revolute"
      3. joint name="waist_pitch_joint" type="revolute"
      4. joint name="left_wrist_pitch_joint" type="fixed"
      5. link name="left_wrist_pitch_link"
      6. joint name="left_wrist_yaw_joint" type="fixed
      7. link name="left_wrist_yaw_link
      8. joint name="left_hand_palm_joint" type="fixed
      9. joint name="right_wrist_pitch_joint" type="fixed"
      10. link name="right_wrist_pitch_link
      11. joint name="right_wrist_yaw_joint" type="fixed"
      12. link name="right_wrist_yaw_link"
      13. joint name="right_hand_palm_joint" type="fixed"
      14. link name="right_rubber_hand"

   3. revolute变为fixed的

      1. joint name="left_wrist_roll_joint" type="revolute"
      2. joint name="right_wrist_roll_joint" type="revolute"

3. 有一些link名字变了





### 经典报错

1. `GLFWError: (65543) b'GLX: Failed to create context: BadValue (integer parameter out of range for operation)' warnings.warn(message, GLFWError)
   ERROR: could not create window`:

   ==conda install -c conda-forge gcc==

2. 





## TODO LIST:

#### 4_25:

1. 把asap的pkl放h2o里看看vis_motion跑的效果，反之也看看(怎么解决的悬空等问题)；        
2. 详细看看23，29等urdf的区别，怎么混一起训             看了，需要改顺序并解决俩joint换了的问题
3. 粗略看一下dagger，瞅一眼怎么实现的如果有时间     不看实现先了



