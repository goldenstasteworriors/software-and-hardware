### 接线

电推杆：绿线(1口)接PB9      黄线(2口)接PB8

控制电平为低时，COM与NC口相连

黄的给高，绿给低会往回拉

黄给低绿给高会往前推

定义满载检测的距离阈值（单位：微米）

串口3（与maixcam通讯） RXD：PC10->PD8 与最旁边的(RX)相连  TXD:PC11-> PD9



6020：黄线接PA15 红线接GND    (这里的颜色指买的线的颜色，下图的颜色指官方线的颜色，只需对照方向)

调大占空比垃圾桶逆时针转

连CH340:黄的连TXD

![image-20241124111650144](../../pictrue/image-20241124111650144.png)



![image-20241116160926646](../../pictrue/image-20241116160926646.png)



舵机：PB6   周期为20ms,范围是0.5ms-2.5ms

增大是逆时针转动



光电：离开是高电平，靠近是低电平



串口屏

PD6 连串口屏TX(蓝色)

PD5 连串口屏RX (绿色)



步进:

1:

ENABLE:PF0（高电平有效）

DIR:PF1

STEP：PA6。



2:

ENABLE:PF2（低电平有效）

DIR:PF3

STEP：PB5 (PA7坏了)



降压模块：

全可调的顺时针电压调大

从外往里分别是:

5V 5V 7V 12V



detect 255







jetson-nano:

引脚读取:

串口通信:

8引脚：TXD

yolov5-5.0 train bug:https://blog.csdn.net/weixin_45498383/article/details/130499508



tsinghua4090:

nohup python train.py --data /home/yekangjie/Downloads/garbage/12_13/data.yaml --cfg yolov5s.yaml --weights '' --batch-size 64 > log/yolov5s_12_13.log 2>&1 &



autodl:

nohup  python train.py --data /root/datasets/yolo_garbage_12_4/data.yaml --cfg yolov5s.yaml --weights '' --batch-size 64 >log/yolov5s_5.0.log 2>&1 &





正确显示垃圾对应的分类信息（格式为：“垃圾类别、数量、分类

成功与否等，如：有害垃圾 1 OK!），每个得 1 分。上述信息出现

任何错误不得分。







（1）有害垃圾：电池（1号、2 号、5 号）、过期药品或内包装等；		3

（2）可回收垃圾：纸杯、100ml 以下塑料瓶或金属罐/瓶；					  0

（3）厨余垃圾：小土豆或土豆块、切过的白萝卜、胡萝卜；					2

（4）其他垃圾：瓷片、鹅卵石、砖块等，												   1

厨余垃圾和其他垃圾的尺寸在一号电池至七号电池之间。每次投入一件垃圾。





### debug日志

1. 3_5 ：调的是video.py，打开了它的cv.imshow，识别的极其离谱，电池都认不得；拍了3_5_test用来看模型效果

   还要动的(除模型)：自启动的模式切换(摄像头对准模式)

   1. `ssl问题`：应该是由于yolov5自动检查更新，需要关掉 

      后发现问题应该在最后几行有个检查更新的代码，注释掉即可

   2. 自启动问题：还没搞定，在

      

2. 3_11:自启动问题 注释了

   ```
   def detect(save_img=False):
       # ...其他代码...
       # 注释视频播放启动
       # video_process = subprocess.Popen(video_command)
       
       # 注释视频终止逻辑
       # if cls and flag:
       #     print("Stopping video...")
       #     video_process.terminate()
       #     video_process = None
       #     flag = 0
       # ...其他代码...
   ```

3. 3_12:

   1. 开机测试：需要开机一分十秒
   2. 自启动问题：在/etc/enviroment和start.service中增加python环境路径

   

2. 





onnx转mud：

```
docker start tpu-env && docker attach tpu-env
cd /root/PubData/

https://wiki.sipeed.com/maixpy/doc/zh/ai_model_converter/maixcam.html

chmod +x convert_yolov5_to_cvimodel.sh && ./convert_yolov5_to_cvimodel.sh
```

mud:

可回收:2 厨余:3  其他:4 有害:5 空白:1





195,160

417,404



move242423out95  242423





```
python classify/train.py --model yolov5n-cls.pt --data imagenette160 --epochs 50 --img 224 --cache
```





523:电池在有害和厨余中跳动；识别为厨余概率极高

242：效果最好，没拍过不认识
